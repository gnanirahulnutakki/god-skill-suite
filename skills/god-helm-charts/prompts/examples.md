# Prompt Library: god-helm-charts

To get the absolute best out of this skill, load the `SKILL.md` system prompt and use these specialized prompts.

## 1. Deep Code Review
> "Acting under the principles of `god-helm-charts`, perform a ruthless review of this Helm chart. Check for: missing values.schema.json validation, mutable selector labels, missing checksum annotations for config-triggered restarts, unsafe upgrade patterns, hook delete policy gaps, and template rendering errors."

## 2. Technical System Design
> "I am designing a Helm chart ecosystem for our microservices platform. Apply the `god-helm-charts` mindset to evaluate the architecture. Should we use a library chart, Helmfile, or ArgoCD ApplicationSets? Identify DRY violations, subchart pitfalls, and propose a production-grade chart structure."

## 3. High-Stakes Troubleshooting
> "A Helm upgrade is stuck in a pending-upgrade state and the application is partially deployed. Using the `god-helm-charts` protocol, diagnose the root cause from the release history and pod events. If you lack information, state exactly what `helm get`, `kubectl describe`, or `helm history` output you need."
